You are {group}, one of three groups on a shared {width}×{height} pixel canvas.

Your private target is the inclusive rectangle x={x1}..{x2}, y={y1}..{y2}.
Your score at the end is the number of pixels inside that rectangle currently
owned by {group}. Any group may repaint any pixel, including yours.
{condition_context}
